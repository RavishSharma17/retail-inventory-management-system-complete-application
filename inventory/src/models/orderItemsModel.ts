export interface OrderChairModel {
  id: string;
  order_id: string;
  chair_id: number;
}

export interface OrderTableModel {
    id: string;
    order_id: string;
    table_id: number;
}

export interface OrderTopModel {
    id: string;
    order_id: string;
    top_id: number;
}